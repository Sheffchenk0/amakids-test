export enum GameSettings {
  COLS = 2,
  PUDS = 5,
  PUDS_SPEED = 600,
}
export enum Ways {
  TOP = 1,
  RIGHT = 2,
  BOTTOM = 3,
  LEFT = 4,
}
export enum GameStatuses {
  STARTED = 1,
  ENDED = 2,
  READY_TO_PICK = 3,
  NOT_STARTED = 4,
}
